# Changesets

This directory contains changesets for gately-auth.

When you make a change to a package, run `pnpm changeset` to create a changeset.
The release workflow uses these to bump versions and update changelogs.

See [CONTRIBUTING.md](../.github/CONTRIBUTING.md) for full details.
