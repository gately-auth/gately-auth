legacy-peer-deps=false
strict-peer-deps=false
save-exact=false
fetch-timeout=300000
fetch-retries=5
# Ensure all platform optional deps are installed (needed for rollup native on Linux CI)
optional=true
